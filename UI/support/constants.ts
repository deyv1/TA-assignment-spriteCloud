/**
 * Purpose: source of truth for constant values.
 */

/** Path where the authenticated browser storage state is persisted/read from. */
export const AUTH_STORAGE_PATH = 'playwright/.auth/ui-auth.json';

/** Minimum number of products the checkout flow requires to be meaningful. */
export const MIN_ITEMS_FOR_CHECKOUT = 2;
